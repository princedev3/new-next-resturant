import ProductsList from "@/components/admin/products-list";
import React from "react";

const page = () => {
  return (
    <div className="w-full">
      <ProductsList />
    </div>
  );
};

export default page;
